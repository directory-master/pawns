{
  "name": "ga-pawns",
  "version": "0.9.10",
  "private": true,
  "description": "Georgia Pawn Shop Directory — zero-backend, SEO-first static directory of pawn shops and valuables buyers across Georgia.",
  "type": "module",
  "scripts": {
    "import": "node scripts/import-csv.mjs",
    "scan": "node scripts/scan-downloads.mjs",
    "stamp": "node scripts/version-stamp.mjs",
    "build:pages": "node scripts/generate-pages.mjs",
    "build": "npm run stamp && npm run build:pages",
    "serve": "python3 -m http.server 8000",
    "dev": "npm run build && npm run serve"
  },
  "license": "UNLICENSED"
}
